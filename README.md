# AutonomousCarGuide

Using UART for pi -> ESP32 communication

pip install -r requirements.txt


sudo apt update
sudo apt install -y python3-pip i2c-tools git
pip install adafruit-circuitpython-bno08x adafruit-blinka smbus2 pyserial RPi.GPIO
